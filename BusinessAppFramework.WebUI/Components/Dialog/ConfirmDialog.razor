@using BusinessAppFramework.Application.Interfaces
@using Contracts
@using MudBlazor
@using BusinessAppFramework.Application.Interfaces

@inject ILocalizedStringService LocalizedStringService

<MudDialog>
    <TitleContent>
        @LocalizedStringService.Get(StringKeys.ConfirmationKey)
    </TitleContent>
    <DialogContent>
        @Message
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Ok">@LocalizedStringService.Get(StringKeys.OkKey)</MudButton>
        <MudButton OnClick="Cancel">@LocalizedStringService.Get(StringKeys.CancelKey)</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [Parameter] public string Message { get; set; } = default!;
    [CascadingParameter] private IMudDialogInstance? MudDialog { get; set; }

    private void Ok() => MudDialog?.Close(DialogResult.Ok(true));

    private void Cancel() => MudDialog?.Cancel();
}

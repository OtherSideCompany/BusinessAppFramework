@using BusinessAppFramework.Application.Interfaces
@using Contracts
@using MudBlazor

@inject ILocalizedStringService LocalizedStringService

<MudDialog>
    <TitleContent>
        @LocalizedStringService.Get(strings.ConfirmationKey)
    </TitleContent>
    <DialogContent>
        @Message
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Ok">@LocalizedStringService.Get(strings.OkKey)</MudButton>
        <MudButton OnClick="Cancel">@LocalizedStringService.Get(strings.CancelKey)</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [Parameter] public string Message { get; set; } = default!;
    [CascadingParameter] private IMudDialogInstance? MudDialog { get; set; }

    private void Ok() => MudDialog?.Close(DialogResult.Ok(true));

    private void Cancel() => MudDialog?.Cancel();
}

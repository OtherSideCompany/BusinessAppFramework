<UserControl x:Class="OtherSideCore.Wpf.UserControls.Editor.DomainObjectReferenceSelectorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OtherSideCore.Wpf.UserControls.Editor"
             xmlns:osc_usercontrols="clr-namespace:OtherSideCore.Wpf.UserControls"
             xmlns:osc_customcontrols="clr-namespace:OtherSideCore.Wpf.CustomControls"
             xmlns:osc_extensions="clr-namespace:OtherSideCore.Wpf.Extensions" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="DomainObjectReferenceSelectorsViewUserControl">

   <Border Margin="16">
      <Grid>

         <TabControl ItemsSource="{Binding}"
                     BorderThickness="0">
            <TabControl.Style>
               <Style TargetType="TabControl">
                  <Setter Property="Template">
                     <Setter.Value>
                        <ControlTemplate TargetType="TabControl">
                           <Grid>
                              <DockPanel>
                                 <Border Background="{StaticResource OtherSide_LightLightGray}" 
                                         DockPanel.Dock="Top"
                                         CornerRadius="8"
                                         Padding="8">
                                    <TabPanel x:Name="HeaderPanel"                                            
                                              IsItemsHost="True" />
                                 </Border>

                                 <ContentPresenter x:Name="PART_SelectedContentHost"
                                                   Margin="2"
                                                   ContentSource="SelectedContent" />
                              </DockPanel>
                           </Grid>
                        </ControlTemplate>
                     </Setter.Value>
                  </Setter>
               </Style>
            </TabControl.Style>
            <TabControl.Resources>
               <Style TargetType="TabItem" x:Key="OtherSideTabItemStyle">
                  <Setter Property="Cursor" Value="Hand" />
                  <Setter Property="Template">
                     <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                           <Grid>
                              <Border x:Name="HeaderBackground"
                                      Background="Transparent"
                                      CornerRadius="8"
                                      Padding="8"
                                      Margin="0,0,4,0">
                                    <ContentPresenter ContentSource="Header" VerticalAlignment="Center" />
                              </Border>
                           </Grid>
                           <ControlTemplate.Triggers>
                              <Trigger Property="IsSelected" Value="True">
                                 <Setter TargetName="HeaderBackground" Property="Background" Value="{StaticResource MMColor_Green}" />
                                 <Setter Property="Foreground" Value="White" />
                              </Trigger>
                              <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="HeaderBackground" Property="Background" Value="{StaticResource MMColor_Green}" />
                                 <Setter Property="Foreground" Value="White" />
                              </Trigger>
                           </ControlTemplate.Triggers>
                        </ControlTemplate>
                     </Setter.Value>
                  </Setter>
               </Style>               
            </TabControl.Resources>

            <TabControl.ItemContainerStyle>
               <Style TargetType="TabItem" BasedOn="{StaticResource OtherSideTabItemStyle}">
                  <Setter Property="Header" Value="{Binding ReferenceTypeName}" />
               </Style>
            </TabControl.ItemContainerStyle>

            <TabControl.ContentTemplate>
               <DataTemplate>
                  <ContentControl Content="{Binding DomainObjectSelectorViewModel}"
                                  ContentTemplateSelector="{Binding DomainObjectReferenceSelectorsView_DataTemplateSelector, ElementName=DomainObjectReferenceSelectorsViewUserControl}"/>
               </DataTemplate>
            </TabControl.ContentTemplate>
         </TabControl>

      </Grid>
   </Border>
</UserControl>
